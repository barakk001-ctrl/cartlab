[phases.setup]
# Node >= 22.13 for the built-in node:sqlite used by store.mjs
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
